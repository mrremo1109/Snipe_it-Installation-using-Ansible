---
- hosts: node1
  user: vagrant
  become: true
  tasks:
   - name: Snipe It Clone
     git:
         repo: https://github.com/snipe/snipe-it.git
         dest: /var/www/snipeit
         update: no
   -  name: Copy file with owner and permissions
      template:
         src: .env
         dest: /var/www/snipeit/.env
vagrant@sim:~$ cat composer_install.yml
---
- hosts: node1
  become: true
  user: vagrant
  tasks:
   - name: Installing Composer
     shell: sudo composer install --no-dev --prefer-source --no-interaction
     args:
       chdir: /var/www/snipeit/

   - name: Permission 1
     shell: sudo chown -R www-data:www-data /var/www/snipeit/

   - name: Permission 2
     shell: sudo chmod -R 755 /var/www/snipeit/
